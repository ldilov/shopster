export const genericPriceObject = {
  type: Number,
  required: true,
  default: 0.0
};

export const genericBooleanStatusObject = {
  type: Boolean,
  required: true,
  default: false
}

export const genericDateAtObject = {
  type: Date,
}
